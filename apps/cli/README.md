# CLI

The Phase 1 CLI lives in the Python package entrypoint:

```bash
python3 -m openmath --help
```

Current commands:

- `openmath init`
- `openmath doctor`
- `openmath backend detect`
- `openmath runs list`
- `openmath graph show`
- `openmath web`
