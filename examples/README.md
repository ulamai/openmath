# Examples

The repository does not yet ship a full mathematical example project.

Create one locally with:

```bash
python3 -m openmath init examples/demo-project
```

That will generate the `.openmath/` workspace, an initial graph node, and an init
run you can inspect in the UI.
